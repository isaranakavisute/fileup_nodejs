const config = {
    nodeEnv       : process.env.NODE_ENV || 'development',
    port          : process.env.PORT || 4000,
    hostname      : process.env.HOSTNAME || "localhost",
    SecretKey     : 'musicagent89567' ,

    

}   

module.exports    = config